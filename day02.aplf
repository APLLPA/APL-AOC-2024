day02←{
    ⍝ part 1

    dif←{2-/⍎⍵}¨⊃⎕NGET'input02.txt' 1
    ⎕←+/{(∧/3≥|⍵)∧(∧/⍵>0)∨(∧/⍵<0)}¨dif

    ⍝ part 2

    input ← ⍎¨⊃⎕NGET'input02.txt' 1
    +/{∨/{(∧/3≥|⍵)∧(∧/⍵>0)∨(∧/⍵<0)}¨{2-/¨(⊂⍵)/⍨¨↓~∘.=⍨⍳≢⍵}⍵}¨input
}
